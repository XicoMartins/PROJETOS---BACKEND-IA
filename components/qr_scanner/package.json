{
  "name": "mtech-qr-scanner",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "esbuild src/scanner.js --bundle --format=esm --minify --outfile=dist/scanner.js"
  },
  "dependencies": {
    "@zxing/browser": "0.2.1"
  },
  "devDependencies": {
    "esbuild": "0.28.1"
  }
}
